name: CI

on:
  pull_request:
  push:
    branches: [main, master, "dev-*"]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Quality checks
    runs-on: ubuntu-latest
    env:
      # Public CDN base (not a secret). Keep in sync with `.env.example`.
      NEXT_PUBLIC_R2_PUBLIC_URL: https://pub-a25eabdea077451bb669dbbc8035ab25.r2.dev
      APP_URL: http://127.0.0.1:3000
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Type check
        run: pnpm typecheck

      - name: Unit tests
        run: pnpm test

      - name: Dependency audit (high+)
        run: pnpm audit:deps

      - name: Build
        run: pnpm build

      - name: Install Playwright Chromium
        run: pnpm exec playwright install chromium --with-deps

      - name: E2E smoke
        run: pnpm exec playwright test
        env:
          CI: true
